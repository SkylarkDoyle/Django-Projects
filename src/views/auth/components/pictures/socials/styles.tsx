import styled from 'styled-components'

export const SocialWrapper = styled.div`
  width: 48px;
  height: 48px;
  border: 1px solid ${({ theme }) => theme.color('text_line')};
  border-radius: 6px;
`
